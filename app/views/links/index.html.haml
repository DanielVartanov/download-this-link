%table{:width => "900px", :border => "0", :align => "center", :bordercolor => "#336699", :cellpadding => "2", :cellspacing => "2" }  
  %tr
    %td
      %table{:width => "900px", :border => "0" }
        %tr{ :align => "Left" }
          %th
            %h2
              Ссылка
          %th
            %h2 
              Состояние
          %th
            %h2
              Размер
          %th
            %h2
              Скорость(б\с)
          %th
            %h2
              Время(с)
          %th
            %h2
              Скачано
          %th
            %h2
              Скачать файл
        - @links.each do |link|
          %tr{ :align => "Left" }
            %td= link_to link.url[0..30], link.url
            %td= link.status
            %td= link.file_size
            %td= link.download_speed
            %td= link.downloading_time
            %td= "#{link.complete_percentage}%"
            %td= link_to 'Файл', absolute_url('files/' + link.file_path) if link.downloaded?